driver-class-name: com.mysql.cj.jdbc.Driver
spring:                                                                 # SOSTITUIRE:
  datasource:                                                           # test_gameez (se volete create sul database per testare)
    url: jdbc:mysql://localhost:3306/gameez_progetto                    #             (inserisci qui accanto il canceletto il nome del progetto originale)
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect